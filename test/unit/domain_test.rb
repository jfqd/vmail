require 'test_helper'

class DomainTest < ActiveSupport::TestCase
  
  test "domain name need to be unique" do
    assert false
  end
  
  test "domain name need to rfc compliant" do
    assert false
  end
  
  test "domain name need in minimun of 3 chars" do
    assert false
  end

  test "tld part need to valid" do
    assert false
  end

end
